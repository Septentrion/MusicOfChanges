<form method="post" action="{{ path('opus_delete', {'id': opus.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ opus.id) }}">
    <button class="btn">Delete</button>
</form>
